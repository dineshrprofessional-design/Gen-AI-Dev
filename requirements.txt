fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
pydantic-settings==2.7.0
httpx==0.28.1

# Document formats. Both are lazy-imported by their extractor, so a missing
# one quarantines that single format rather than breaking the whole ingest.
pypdf==6.15.0
python-docx==1.2.0

# Gold question set
pyyaml==6.0.3

# Retrieval. bge-m3 is ~2.2GB of weights downloaded on first use; both are
# imported lazily so nothing here loads unless an index is actually built.
sentence-transformers==5.1.2
chromadb==1.5.9
